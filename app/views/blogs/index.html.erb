<h1>今日の一言一覧</h1>
<p><%= notice %></p>
<table>
  <tr></tr>
    <th>内容</th>
  </tr>

  <% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.content %></td>
    <td><%= link_to '詳細を確認する' , blog_path(blog.id) %></td>
    <td><%= link_to "今日の一言を編集する ", edit_blog_path(blog.id), data:{ confirm: '本当に編集していいですか?'} %></td>
    <td><%= link_to '今日の一言を削除する', blog_path(blog.id), method: :delete, data:{confirm: '本当に削除してよろしいでしょうか?'} %></td>
  </tr>
<% end %>
    </table>